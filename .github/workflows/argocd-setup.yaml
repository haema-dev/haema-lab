name: "Step 2: ArgoCD Bootstrapping"

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - ".github/workflows/argocd-setup.yaml"

jobs:
  setup-argocd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🌎 Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          version: 1.94.1
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}

      - name: 🏗️ Install ArgoCD & Register Root App
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.PROXMOX_VM_TAILSCALE_IP }}
          username: ${{ secrets.USERNAME }}
          # 🚨 중요: bootstrap.yml의 Phase 2에서 등록한 전용 키를 사용해야 합니다.
          key: ${{ secrets.SSH_USERNAME_PRIVATE_KEY }} 
          script: |
            set -e
            # bootstrap.yml에서 설정한 KUBECONFIG 경로를 명시합니다.
            export KUBECONFIG=$HOME/.kube/config

            # 1. ArgoCD 설치
            echo "🚀 Installing ArgoCD..."
            kubectl create namespace argocd || true
            kubectl apply --server-side -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

            # 2. ArgoCD 대기 (Ready 상태 확인)
            echo "⏳ Waiting for ArgoCD components..."
            kubectl wait --for=condition=Ready pod -l app.kubernetes.io/part-of=argocd -n argocd --timeout=300s

            # 3. Grafana Secret 주입 (Monitoring 네임스페이스)
            echo "🔐 Injecting Grafana Credentials..."
            kubectl create namespace monitoring || true
            kubectl create secret generic grafana-admin-credentials \
              --from-literal=admin-password='${{ secrets.GRAFANA_PASSWORD }}' \
              -n monitoring \
              --dry-run=client -o yaml | kubectl apply -f -

            # 4. Root Application 등록
            echo "📂 Applying Root Application..."
            # 변수를 활용하여 현재 리포지토리의 master 브랜치에서 root-app.yaml을 가져옵니다.
            REPO_OWNER=$(echo "${{ github.repository }}" | cut -d'/' -f1)
            REPO_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2)
            
            curl -s https://raw.githubusercontent.com/${REPO_OWNER}/${REPO_NAME}/master/argocd/root-app.yaml | kubectl apply -f -

            echo "✅ ArgoCD Setup & Root App Registration Complete!"